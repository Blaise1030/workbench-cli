name: Deploy GitHub Pages (disabled)
# Replaced by deploy-landing-page.yml — Cloudflare Workers deployment
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deploy is disabled. See deploy-landing-page.yml."
