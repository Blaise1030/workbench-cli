# Implementation Plans

Generated by the `improve` skill on 2026-06-13 (HEAD `9f5483a`). Execute in
the order below — all three are independent and can run in parallel. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update your status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-fix-ratelimiter-memory-leak.md) | Fix RateLimiter memory leak — evict expired entries | P1 | S | — | TODO |
| [002](002-fix-checkout-mutation-stale-branches.md) | Fix checkout mutation: use apiClient + invalidate branches | P1 | S | — | TODO |
| [003](003-fix-silent-sync-error-in-grouped-worktrees.md) | Log sync errors in ListAllWorktreesGrouped | P1 | S | — | TODO |
| [004](004-http-integration-tests-workspace-router.md) | HTTP integration tests for workspace router (auth, CORS, path guard) | P2 | M | — | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (rationale)`

## Dependency notes

No dependencies between plans. All three can be worked in parallel on separate
branches.

## Findings considered and rejected

- **Path traversal in file ops**: `path_guard.go:AssertPathWithinRoot` uses `filepath.Abs` + `filepath.Rel` — correct containment; not a finding.
- **`PRAGMA table_info` string concatenation in `db/migrate.go`**: table/column values are hardcoded Go constants, no external input path — not exploitable.
- **WebSocket loopback auth bypass**: intentional auto-activation behaviour for localhost-only tool — by design.
- **`drop-assets` no per-file size limit**: deferred by owner; not planned.
- **`drop-assets` returns absolute server paths**: low impact for a localhost tool; not planned.
- **Settings → SQLite**: file-based JSON settings is intentional — users can manually edit the file to override values. By design; not planned.
- **Hand-rolled schema migration**: external migration library (golang-migrate, goose) rejected to keep binary size minimal. Owner will add a `schema_version` tracking table to the existing hand-rolled system without external dependencies.
- **No HTTP-level handler tests**: real gap but M effort; deferred.
